{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Gradlew Client",
      "preLaunchTask": "gradlew client",
      "program": "${workspaceFolder}"
    },
    {
      "type": "node",
      "request": "launch",
      "name": "Gradlew Server",
      "preLaunchTask": "gradlew server",
      "program": "${workspaceFolder}"
    },
    
  ]
}